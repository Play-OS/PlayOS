import React, { ReactElement } from 'react';
import HeaderContainer from '../../containers/HeaderContainer';


export default function ConnectedHeader(): ReactElement {
    return (
        <HeaderContainer />
    );
}
